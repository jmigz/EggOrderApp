'use strict';

module.exports = {
  up: async (queryInterface, Sequelize) => {
    
  },

  down: async (queryInterface, Sequelize) => {
    // If needed, provide a way to revert the default value change
    // For example:
    // await queryInterface.sequelize.query(
    //   'ALTER TABLE "Orders" ALTER COLUMN "balance" SET DEFAULT 0'
    // );
  },
};
